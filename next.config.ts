import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Public menu uses <img> for owner logo URLs so arbitrary hosts work without listing each CDN. */
};

export default nextConfig;
